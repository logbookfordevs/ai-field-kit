interface:
  display_name: "Fork Skill"
  short_description: "Create or update a literal skill fork"
  default_prompt: "Use $fork-skill to create or update a literal skill fork with only necessary local adaptations."
policy:
  allow_implicit_invocation: false
