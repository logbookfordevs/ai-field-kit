interface:
  display_name: "Pickup"
  short_description: "Resume from temp-directory handoff notes."
  default_prompt: "Use $afk-pickup to find and resume from the latest relevant temp-directory handoff."
