interface:
  display_name: "AFK Sprint"
  short_description: "Fast goal execution with Markdown checkpoint tracking"
  default_prompt: "Use $afk-sprint for explicit AFK Sprint requests: prepare the goal with Plannotator, fragment it into AFK checkpoint packets, run native /goal, and track execution with AFK execution tracking."
