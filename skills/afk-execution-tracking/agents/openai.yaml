interface:
  display_name: "Execution Tracking"
  short_description: "Track checkpointed implementation state across tasks, reviews, validation, and agent handoffs."
  default_prompt: "Track this implementation using AFK execution tracking: resume from existing checkpoint packet files, keep packet statuses current, record validation evidence and review gates, preserve handoff notes, and keep implementation notes clear."
