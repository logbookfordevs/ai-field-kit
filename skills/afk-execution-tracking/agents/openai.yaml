interface:
  display_name: "Execution Tracking"
  short_description: "Track checkpointed implementation state across tasks, reviews, validation, and agent handoffs."
  default_prompt: "Track this implementation using the AFK execution tracking workflow: create or update the canonical tracking file, keep task status current, record validation and review gates, and preserve a clear next action for resume."
