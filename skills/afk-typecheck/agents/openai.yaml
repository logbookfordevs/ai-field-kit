display_name: AFK Typecheck
short_description: Run TypeScript type checking, document failures in a temporary `typecheck.md`, fix the issues, and close the loop with the user before optionally deleting the report.
default_prompt: Follow the AI Field Kit /afk-typecheck workflow for this project.
policy:
  allow_implicit_invocation: false
