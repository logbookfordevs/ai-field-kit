# Review-Gated Turbo

Keep Turbo running, but do not auto-commit task code changes.
Instead, stage the task changes and before moving it to done, wait the user review.
Suggest Plannotator Review for a better review experience: `/plannotator-review`.
Continue only after the user approves, commits, requests changes, or explicitly skips the gate.