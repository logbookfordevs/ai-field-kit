interface:
  display_name: "AFK Resume Workflow"
  short_description: "Resume an AFK workflow from repo artifacts and route the next move."
  default_prompt: "Use $afk-resume-workflow to continue the current AFK workflow from durable repo artifacts and route the next useful phase through $afk-compass."
