interface:
  display_name: "AFK Resume Workflow"
  short_description: "Resume feature or AFK workflow work from repo artifacts."
  default_prompt: "Use $afk-resume-workflow to continue the current feature or AFK workflow from durable repo artifacts, honor active execution tracking, and use $afk-compass only when the next useful skill is unclear."
