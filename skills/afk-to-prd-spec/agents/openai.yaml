interface:
  display_name: "AFK To PRD Spec"
  short_description: "Create or normalize an agent-ready PRD/spec"
  default_prompt: "Use $afk-to-prd-spec to turn grilled context, a PM PRD, or rough feature notes into a local-first, tracker-aware PRD/spec artifact with product intent, behavior, acceptance criteria, implementation decisions, and testing seams."
