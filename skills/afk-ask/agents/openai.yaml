interface:
  display_name: "Ask"
  short_description: "Ask a local Claude, Codex, or Gemini CLI for a second opinion and preserve the answer."
  default_prompt: "Ask a local AI CLI advisor for a focused second opinion: choose Claude, Codex, or Gemini; run the matching read-only non-interactive command; save the raw answer and summary as an artifact using the repo convention."
